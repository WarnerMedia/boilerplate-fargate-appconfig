version: "3"
services:
  #2022.09.03: This build is taking about 10 minutes to run, seems to hang for about five minutes during the Python install.
  codebuild:
    build: .
    stdin_open: true
    tty: true
